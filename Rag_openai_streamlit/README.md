# OpenAI + Qdrant + Streamlit RAG

## Setup

1. Create .env file

OPENAI_API_KEY=your_key

2. Install

pip install -r requirements.txt

3. Start Qdrant

docker run -p 6333:6333 qdrant/qdrant

4. Run

streamlit run app.py

5. Upload PDF and chat.
